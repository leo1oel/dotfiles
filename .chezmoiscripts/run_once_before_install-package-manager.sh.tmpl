#!/bin/bash

set -e

{{ if eq .chezmoi.os "darwin" -}}
#################
# macOS - Homebrew
#################

# Check if Homebrew is installed
if command -v brew >/dev/null 2>&1; then
    echo "✅ Homebrew is already installed at $(command -v brew)"
else
    echo "🚀 Homebrew not found. Installing..."
    /bin/bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"

    # Add Homebrew to PATH depending on architecture
    if [[ -d "/opt/homebrew/bin" ]]; then
        echo 'export PATH="/opt/homebrew/bin:$PATH"' >> ~/.bash_profile
        export PATH="/opt/homebrew/bin:$PATH"
    elif [[ -d "/usr/local/bin" ]]; then
        echo 'export PATH="/usr/local/bin:$PATH"' >> ~/.bash_profile
        export PATH="/usr/local/bin:$PATH"
    fi

    echo "✅ Homebrew installation complete."
fi

#################
# Fish Shell
#################
if command -v fish >/dev/null 2>&1; then
    echo "✅ Fish shell is already installed at $(command -v fish)"
else
    echo "🚀 Fish shell not found. Installing..."
    brew install fish
    echo "Your password may be required to enable the Fish shell..."
    sudo sh -c "echo /opt/homebrew/bin/fish >> /etc/shells"
    chsh -s /opt/homebrew/bin/fish
fi

#################
# yazi (terminal file manager)
#################
if ! command -v yazi >/dev/null 2>&1; then
    echo "📦 Installing yazi..."
    brew install yazi
    echo "✅ yazi installed via Homebrew"
fi

#################
# Oh My Tmux
#################
OHMY_TMUX_BASE="${XDG_CONFIG_HOME:-$HOME/.config}/tmux"
OHMY_TMUX_DIR="$OHMY_TMUX_BASE/oh-my-tmux"
echo "📦 Installing Oh My Tmux (force overwrite)..."
rm -f "$OHMY_TMUX_BASE/tmux.conf" "$OHMY_TMUX_BASE/tmux.conf.local" "$HOME/.tmux.conf"
rm -rf "$OHMY_TMUX_DIR"
mkdir -p "$OHMY_TMUX_BASE"
git clone --single-branch https://github.com/gpakosz/.tmux.git "$OHMY_TMUX_DIR"
ln -sf "$OHMY_TMUX_DIR/.tmux.conf" "$OHMY_TMUX_BASE/tmux.conf"
cp -f "$OHMY_TMUX_DIR/.tmux.conf.local" "$OHMY_TMUX_BASE/tmux.conf.local"
echo "✅ Oh My Tmux installed to $OHMY_TMUX_BASE"

{{ else if eq .chezmoi.os "linux" -}}
#################
# Linux - APT
#################

echo "🐧 Setting up Linux environment..."

# Ensure user-local tool paths are available in this script run
export PATH="$HOME/.local/bin:$HOME/.cargo/bin:$PATH"

# Check if we have sudo privileges
HAS_SUDO=false
if sudo -n true 2>/dev/null; then
    HAS_SUDO=true
    echo "✅ Running with sudo privileges"
else
    echo "ℹ️  Running without sudo (user-space installation)"
fi

# Update package lists (only if we have sudo)
if [ "$HAS_SUDO" = true ]; then
    echo "📦 Updating package lists..."
    sudo apt update
fi

#################
# Install required packages for PPA management
#################
if [ "$HAS_SUDO" = true ]; then
    echo "📦 Installing software-properties-common..."
    sudo apt install -y software-properties-common
fi

#################
# Git (latest version via PPA)
#################
if [ "$HAS_SUDO" = true ]; then
    echo "🔧 Adding Git PPA for latest version..."
    sudo apt-add-repository ppa:git-core/ppa -y
    sudo apt update
    echo "📦 Installing/upgrading Git..."
    sudo apt install -y git
    echo "✅ Git $(git --version | awk '{print $3}') installed"
else
    echo "ℹ️  Using system Git (may be outdated)"
fi

#################
# Fish Shell
#################
if command -v fish >/dev/null 2>&1; then
    echo "✅ Fish shell is already installed at $(command -v fish)"
    FISH_PATH=$(which fish)

    # Ensure cd.fish exists (required by zoxide)
    mkdir -p "$HOME/.local/share/fish/functions"
    if [ ! -f "$HOME/.local/share/fish/functions/cd.fish" ]; then
        cat > "$HOME/.local/share/fish/functions/cd.fish" << 'CDEOF'
function cd --description 'Change directory'
    builtin cd $argv
end
CDEOF
        echo "✅ Created cd.fish function for zoxide compatibility"
    fi
else
    if [ "$HAS_SUDO" = true ]; then
        echo "🚀 Installing Fish shell via PPA..."
        sudo apt-add-repository ppa:fish-shell/release-3 -y
        sudo apt update
        sudo apt install fish -y
        FISH_PATH=$(which fish)
        echo "✅ Fish shell installation complete."

        # Create missing cd.fish function (required by zoxide)
        mkdir -p "$HOME/.local/share/fish/functions"
        if [ ! -f "$HOME/.local/share/fish/functions/cd.fish" ]; then
            cat > "$HOME/.local/share/fish/functions/cd.fish" << 'CDEOF'
function cd --description 'Change directory'
    builtin cd $argv
end
CDEOF
            echo "✅ Created cd.fish function"
        fi
    else
        # Try to install Fish to ~/.local without sudo
        echo "🚀 Installing Fish shell to ~/.local (no sudo)..."
        mkdir -p "$HOME/.local/bin"

        # Use official standalone binary from GitHub releases
        # These are single-file binaries that work on any Linux
        FISH_VERSION="4.1.2"
        FISH_URL="https://github.com/fish-shell/fish-shell/releases/download/${FISH_VERSION}/fish-${FISH_VERSION}-linux-x86_64.tar.xz"

        cd /tmp
        FISH_INSTALLED=false

        # Download standalone binary
        echo "📦 Downloading Fish ${FISH_VERSION} standalone binary..."
        if command -v wget >/dev/null 2>&1; then
            wget -q "$FISH_URL" -O fish.tar.xz 2>/dev/null && DOWNLOAD_OK=true || DOWNLOAD_OK=false
        elif command -v curl >/dev/null 2>&1; then
            curl -sL "$FISH_URL" -o fish.tar.xz 2>/dev/null && DOWNLOAD_OK=true || DOWNLOAD_OK=false
        else
            echo "⚠️  wget or curl not available. Cannot download Fish."
            DOWNLOAD_OK=false
        fi

        if [ "$DOWNLOAD_OK" = true ] && [ -f fish.tar.xz ]; then
            echo "📦 Extracting Fish binary..."

            # Extract the standalone binary
            tar -xf fish.tar.xz 2>/dev/null

            # The extracted file should be just 'fish'
            if [ -f fish ]; then
                chmod +x fish
                mv fish "$HOME/.local/bin/fish"
                FISH_INSTALLED=true
            fi

            # Cleanup
            rm -f fish.tar.xz fish
        fi

        if [ "$FISH_INSTALLED" = true ] && [ -x "$HOME/.local/bin/fish" ]; then
            FISH_PATH="$HOME/.local/bin/fish"
            echo "✅ Fish ${FISH_VERSION} installed to ~/.local/bin"

            # Initialize Fish data files on first run
            echo "📦 Initializing Fish data files..."
            export PATH="$HOME/.local/bin:$PATH"
            if echo "exit" | "$FISH_PATH" >/dev/null 2>&1; then
                echo "✅ Fish data files initialized"
            fi

            # Create missing cd.fish function (required by zoxide)
            mkdir -p "$HOME/.local/share/fish/functions"
            if [ ! -f "$HOME/.local/share/fish/functions/cd.fish" ]; then
                cat > "$HOME/.local/share/fish/functions/cd.fish" << 'CDEOF'
function cd --description 'Change directory'
    builtin cd $argv
end
CDEOF
                echo "✅ Created cd.fish function"
            fi
        else
            echo "⚠️  Could not install Fish without sudo."
            echo "   Please ask your system administrator to run:"
            echo "   sudo apt install fish"
            FISH_PATH=""
        fi
    fi
fi

# Ensure ~/.local/bin is in PATH for all shells
if [ -d "$HOME/.local/bin" ]; then
    # Add to .profile (used by bash, sh, and login shells)
    if [ -f "$HOME/.profile" ]; then
        if ! grep -q 'PATH.*\.local/bin' "$HOME/.profile" 2>/dev/null; then
            cat >> "$HOME/.profile" << 'EOF'

# Add ~/.local/bin to PATH
if [ -d "$HOME/.local/bin" ] ; then
    PATH="$HOME/.local/bin:$PATH"
fi
EOF
        fi
    fi

    # Add to .bashrc (for bash)
    if [ -f "$HOME/.bashrc" ]; then
        if ! grep -q 'PATH.*\.local/bin' "$HOME/.bashrc" 2>/dev/null; then
            cat >> "$HOME/.bashrc" << 'EOF'

# Add ~/.local/bin to PATH
if [ -d "$HOME/.local/bin" ] ; then
    PATH="$HOME/.local/bin:$PATH"
fi
EOF
        fi
    fi

    # Add to .zshrc (for zsh)
    if [ -f "$HOME/.zshrc" ]; then
        if ! grep -q 'PATH.*\.local/bin' "$HOME/.zshrc" 2>/dev/null; then
            cat >> "$HOME/.zshrc" << 'EOF'

# Add ~/.local/bin to PATH
if [ -d "$HOME/.local/bin" ] ; then
    PATH="$HOME/.local/bin:$PATH"
fi
EOF
        fi
    fi
fi

# Set Fish as default shell (only if Fish is available)
if [ -n "$FISH_PATH" ] && [ -x "$FISH_PATH" ]; then
    echo "🐚 Setting Fish as default shell..."

    if [ "$HAS_SUDO" = true ]; then
        # With sudo: add to /etc/shells and change default shell
        if ! grep -q "$FISH_PATH" /etc/shells 2>/dev/null; then
            echo "$FISH_PATH" | sudo tee -a /etc/shells >/dev/null
        fi

        # Check current default shell
        CURRENT_SHELL=$(getent passwd "$USER" 2>/dev/null | cut -d: -f7)
        if [ "$CURRENT_SHELL" != "$FISH_PATH" ]; then
            if chsh -s "$FISH_PATH" 2>/dev/null; then
                echo "✅ Fish set as default shell. Please logout and login again."
            else
                echo "ℹ️  Could not set Fish as default shell"
                echo "   Try: sudo chsh -s $FISH_PATH $USER"
            fi
        else
            echo "✅ Fish is already the default shell."
        fi
    else
        # Without sudo: can't change default shell, but can auto-start Fish
        echo "ℹ️  Without sudo, cannot set Fish as system default shell."
        echo "   Fish will auto-start from bash via ~/.bashrc"

        # Add Fish auto-start to .bashrc if not already there
        if [ -f "$HOME/.bashrc" ]; then
            if ! grep -q "exec.*fish" "$HOME/.bashrc" 2>/dev/null; then
                cat >> "$HOME/.bashrc" << 'EOF'

# Auto-start Fish shell if available
if [ -x "$HOME/.local/bin/fish" ] && [ -z "$FISH_VERSION" ]; then
    exec "$HOME/.local/bin/fish"
fi
EOF
                echo "✅ Added Fish auto-start to ~/.bashrc"
            fi
        fi
    fi
fi

#################
# Essential tools (via curl installers)
#################
echo "🔧 Installing essential tools via curl installers..."

# Starship prompt
if ! command -v starship >/dev/null 2>&1; then
    echo "🚀 Installing Starship prompt..."
    mkdir -p "$HOME/.local/bin"
    if [ "$HAS_SUDO" = true ]; then
        # Install to /usr/local/bin with sudo
        curl -sS https://starship.rs/install.sh | sh -s -- -y
    else
        # Install to ~/.local/bin without sudo
        curl -sS https://starship.rs/install.sh | sh -s -- -y -b "$HOME/.local/bin"
    fi
    echo "✅ Starship installed"
fi

# uv (Python package manager)
if ! command -v uv >/dev/null 2>&1; then
    echo "📦 Installing uv..."
    curl -LsSf https://astral.sh/uv/install.sh | sh
    echo "✅ uv installed"
fi

#################
# Neovim (ensure >= 0.8 for LazyVim)
#################
NEEDS_NVIM_INSTALL=true
if command -v nvim >/dev/null 2>&1; then
    NVIM_VERSION_LINE=$(nvim --version 2>/dev/null | head -n1)
    NVIM_VERSION_MAJOR=$(echo "$NVIM_VERSION_LINE" | sed -E 's/.*v([0-9]+)\.([0-9]+).*/\1/')
    NVIM_VERSION_MINOR=$(echo "$NVIM_VERSION_LINE" | sed -E 's/.*v([0-9]+)\.([0-9]+).*/\2/')
    if [ -n "$NVIM_VERSION_MAJOR" ] && [ -n "$NVIM_VERSION_MINOR" ]; then
        if [ "$NVIM_VERSION_MAJOR" -gt 0 ] || [ "$NVIM_VERSION_MINOR" -ge 8 ]; then
            NEEDS_NVIM_INSTALL=false
            echo "✅ Neovim ${NVIM_VERSION_MAJOR}.${NVIM_VERSION_MINOR} already meets minimum version"
        else
            echo "⚠️  Neovim ${NVIM_VERSION_MAJOR}.${NVIM_VERSION_MINOR} is too old (< 0.8)"
        fi
    fi
fi

if [ "$NEEDS_NVIM_INSTALL" = true ]; then
    echo "📦 Installing newer Neovim from GitHub release..."
    mkdir -p "$HOME/.local/bin" "$HOME/.local/opt"

    ARCH=$(uname -m)
    NVIM_ASSET=""
    if [ "$ARCH" = "x86_64" ]; then
        NVIM_ASSET="nvim-linux-x86_64.tar.gz"
    elif [ "$ARCH" = "aarch64" ] || [ "$ARCH" = "arm64" ]; then
        NVIM_ASSET="nvim-linux-arm64.tar.gz"
    else
        echo "⚠️  Unsupported architecture for automatic Neovim install: $ARCH"
    fi

    if [ -n "$NVIM_ASSET" ]; then
        NVIM_URL="https://github.com/neovim/neovim/releases/latest/download/${NVIM_ASSET}"
        if ! curl -fL "$NVIM_URL" -o /tmp/nvim.tar.gz; then
            if [ "$ARCH" = "x86_64" ]; then
                NVIM_URL="https://github.com/neovim/neovim/releases/latest/download/nvim-linux64.tar.gz"
                curl -fL "$NVIM_URL" -o /tmp/nvim.tar.gz || true
            fi
        fi

        if [ -s /tmp/nvim.tar.gz ]; then
            rm -rf /tmp/nvim-linux* /tmp/nvim.appimage
            tar -xzf /tmp/nvim.tar.gz -C /tmp
            NVIM_DIR=$(find /tmp -maxdepth 1 -type d -name "nvim-linux*" | head -n1)
            if [ -n "$NVIM_DIR" ] && [ -x "$NVIM_DIR/bin/nvim" ]; then
                rm -rf "$HOME/.local/opt/nvim"
                mv "$NVIM_DIR" "$HOME/.local/opt/nvim"
                ln -sf "$HOME/.local/opt/nvim/bin/nvim" "$HOME/.local/bin/nvim"
                echo "✅ Neovim installed to ~/.local/bin/nvim"
            else
                echo "⚠️  Failed to extract a valid Neovim binary from archive"
            fi
            rm -f /tmp/nvim.tar.gz
        else
            echo "⚠️  Could not download Neovim release asset"
        fi
    fi
fi

#################
# eza (better ls)
#################
if ! command -v eza >/dev/null 2>&1; then
    echo "📦 Installing eza..."
    mkdir -p "$HOME/.local/bin"
    wget -c https://github.com/eza-community/eza/releases/latest/download/eza_x86_64-unknown-linux-gnu.tar.gz -O - | tar xz
    chmod +x eza
    mv eza "$HOME/.local/bin/eza"
    echo "✅ eza installed to ~/.local/bin"
fi

#################
# git-delta (better git diff)
#################
if ! command -v delta >/dev/null 2>&1; then
    echo "📦 Installing git-delta..."
    mkdir -p "$HOME/.local/bin"
    DELTA_VERSION=$(curl -s https://api.github.com/repos/dandavison/delta/releases/latest | grep '"tag_name"' | sed -E 's/.*"([^"]+)".*/\1/')
    if [ -n "$DELTA_VERSION" ]; then
        DELTA_URL="https://github.com/dandavison/delta/releases/download/${DELTA_VERSION}/delta-${DELTA_VERSION}-x86_64-unknown-linux-gnu.tar.gz"
        if wget -q "$DELTA_URL" -O /tmp/delta.tar.gz; then
            tar -xzf /tmp/delta.tar.gz -C /tmp
            mv "/tmp/delta-${DELTA_VERSION}-x86_64-unknown-linux-gnu/delta" "$HOME/.local/bin/"
            chmod +x "$HOME/.local/bin/delta"
            rm -rf /tmp/delta.tar.gz "/tmp/delta-${DELTA_VERSION}-x86_64-unknown-linux-gnu"
            echo "✅ git-delta installed to ~/.local/bin"
        else
            echo "⚠️  Could not download git-delta from ${DELTA_URL}"
            rm -f /tmp/delta.tar.gz
        fi
    else
        echo "⚠️  Could not determine git-delta version (GitHub API may be rate-limited). Skipping."
    fi
fi

#################
# GitHub CLI (gh)
#################
if ! command -v gh >/dev/null 2>&1; then
    echo "📦 Installing GitHub CLI (gh)..."
    if [ "$HAS_SUDO" = true ]; then
        # Install via official GitHub CLI apt repository
        sudo mkdir -p /etc/apt/keyrings
        curl -fsSL https://cli.github.com/packages/githubcli-archive-keyring.gpg | sudo dd of=/etc/apt/keyrings/githubcli-archive-keyring.gpg >/dev/null
        sudo chmod go+r /etc/apt/keyrings/githubcli-archive-keyring.gpg
        echo "deb [arch=$(dpkg --print-architecture) signed-by=/etc/apt/keyrings/githubcli-archive-keyring.gpg] https://cli.github.com/packages stable main" | sudo tee /etc/apt/sources.list.d/github-cli.list >/dev/null
        sudo apt update
        sudo apt install -y gh
        echo "✅ GitHub CLI installed via apt"
    else
        # Install latest release to ~/.local/bin without sudo
        mkdir -p "$HOME/.local/bin"
        GH_VERSION=$(curl -s https://api.github.com/repos/cli/cli/releases/latest | grep '"tag_name"' | sed -E 's/.*"v?([^"]+)".*/\1/')
        wget "https://github.com/cli/cli/releases/download/v${GH_VERSION}/gh_${GH_VERSION}_linux_amd64.tar.gz" -O /tmp/gh.tar.gz
        tar -xzf /tmp/gh.tar.gz -C /tmp
        mv "/tmp/gh_${GH_VERSION}_linux_amd64/bin/gh" "$HOME/.local/bin/"
        chmod +x "$HOME/.local/bin/gh"
        rm -rf /tmp/gh.tar.gz "/tmp/gh_${GH_VERSION}_linux_amd64"
        echo "✅ GitHub CLI installed to ~/.local/bin"
    fi
fi

#################
# lazygit (git TUI)
#################
if ! command -v lazygit >/dev/null 2>&1; then
    echo "📦 Installing lazygit..."
    mkdir -p "$HOME/.local/bin"
    LAZYGIT_VERSION=$(curl -s "https://api.github.com/repos/jesseduffield/lazygit/releases/latest" | grep -Po '"tag_name": "v\K[^"]*')
    wget "https://github.com/jesseduffield/lazygit/releases/latest/download/lazygit_${LAZYGIT_VERSION}_Linux_x86_64.tar.gz" -O /tmp/lazygit.tar.gz
    tar -xzf /tmp/lazygit.tar.gz -C /tmp
    mv /tmp/lazygit "$HOME/.local/bin/"
    chmod +x "$HOME/.local/bin/lazygit"
    rm /tmp/lazygit.tar.gz
    echo "✅ lazygit installed to ~/.local/bin"
fi

#################
# fzf (fuzzy finder)
#################
if ! command -v fzf >/dev/null 2>&1; then
    echo "📦 Installing fzf..."
    mkdir -p "$HOME/.local/bin"
    FZF_VERSION=$(curl -s https://api.github.com/repos/junegunn/fzf/releases/latest | grep '"tag_name"' | sed -E 's/.*"v?([^"]+)".*/\1/')
    wget "https://github.com/junegunn/fzf/releases/download/v${FZF_VERSION}/fzf-${FZF_VERSION}-linux_amd64.tar.gz" -O /tmp/fzf.tar.gz 2>/dev/null
    tar -xzf /tmp/fzf.tar.gz -C /tmp
    mv /tmp/fzf "$HOME/.local/bin/"
    chmod +x "$HOME/.local/bin/fzf"
    rm /tmp/fzf.tar.gz
    echo "✅ fzf installed to ~/.local/bin"
fi

#################
# zoxide (smarter cd)
#################
if ! command -v zoxide >/dev/null 2>&1; then
    echo "📦 Installing zoxide..."
    mkdir -p "$HOME/.local/bin"
    ZOXIDE_VERSION=$(curl -s https://api.github.com/repos/ajeetdsouza/zoxide/releases/latest | grep '"tag_name"' | sed -E 's/.*"v?([^"]+)".*/\1/')
    wget "https://github.com/ajeetdsouza/zoxide/releases/download/v${ZOXIDE_VERSION}/zoxide-${ZOXIDE_VERSION}-x86_64-unknown-linux-musl.tar.gz" -O /tmp/zoxide.tar.gz 2>/dev/null
    tar -xzf /tmp/zoxide.tar.gz -C /tmp
    mv /tmp/zoxide "$HOME/.local/bin/"
    chmod +x "$HOME/.local/bin/zoxide"
    rm /tmp/zoxide.tar.gz
    echo "✅ zoxide installed to ~/.local/bin"
fi

#################
# neofetch (system info)
#################
if ! command -v neofetch >/dev/null 2>&1; then
    echo "📦 Installing neofetch..."
    mkdir -p "$HOME/.local/bin"
    # neofetch is just a bash script, download directly from GitHub
    curl -fsSL "https://raw.githubusercontent.com/dylanaraps/neofetch/master/neofetch" -o "$HOME/.local/bin/neofetch"
    chmod +x "$HOME/.local/bin/neofetch"
    echo "✅ neofetch installed to ~/.local/bin"
fi

# Remove old fastfetch if it exists (causes GLIBC errors on older systems)
if [ -f "$HOME/.local/bin/fastfetch" ]; then
    rm -f "$HOME/.local/bin/fastfetch"
    echo "🧹 Removed old fastfetch binary"
fi

#################
# yazi (terminal file manager)
#################
if ! command -v yazi >/dev/null 2>&1; then
    echo "📦 Installing yazi..."
    mkdir -p "$HOME/.local/bin"
    YAZI_VERSION=$(curl -s https://api.github.com/repos/sxyazi/yazi/releases/latest | grep '"tag_name"' | sed -E 's/.*"([^"]+)".*/\1/')
    if [ -n "$YAZI_VERSION" ]; then
        YAZI_BASE_URL="https://github.com/sxyazi/yazi/releases/download/${YAZI_VERSION}"
        if wget -q "${YAZI_BASE_URL}/yazi-x86_64-unknown-linux-musl.zip" -O /tmp/yazi.zip; then
            :
        elif wget -q "${YAZI_BASE_URL}/yazi-x86_64-unknown-linux-gnu.zip" -O /tmp/yazi.zip; then
            :
        else
            echo "⚠️  Could not download yazi release assets from ${YAZI_BASE_URL}"
            rm -f /tmp/yazi.zip
        fi
        if [ -s /tmp/yazi.zip ]; then
            rm -rf /tmp/yazi
            mkdir -p /tmp/yazi
            unzip -o /tmp/yazi.zip -d /tmp/yazi >/dev/null 2>&1
            YAZI_BIN=$(find /tmp/yazi -type f -name yazi | head -n1)
            YA_BIN=$(find /tmp/yazi -type f -name ya | head -n1)
            if [ -n "$YAZI_BIN" ] && [ -n "$YA_BIN" ]; then
                mv "$YAZI_BIN" "$HOME/.local/bin/yazi"
                mv "$YA_BIN" "$HOME/.local/bin/ya"
                chmod +x "$HOME/.local/bin/yazi" "$HOME/.local/bin/ya"
                echo "✅ yazi installed to ~/.local/bin"
            else
                echo "⚠️  yazi archive extracted, but binaries were not found"
            fi
            rm -rf /tmp/yazi /tmp/yazi.zip
        fi
    else
        echo "⚠️  Could not determine yazi version (GitHub API may be rate-limited). Skipping."
    fi
fi

#################
# direnv (environment switcher)
#################
if ! command -v direnv >/dev/null 2>&1; then
    echo "📦 Installing direnv..."
    mkdir -p "$HOME/.local/bin"
    DIRENV_VERSION=$(curl -s https://api.github.com/repos/direnv/direnv/releases/latest | grep '"tag_name"' | sed -E 's/.*"v([^"]+)".*/\1/')
    wget "https://github.com/direnv/direnv/releases/download/v${DIRENV_VERSION}/direnv.linux-amd64" -O "$HOME/.local/bin/direnv"
    chmod +x "$HOME/.local/bin/direnv"
    echo "✅ direnv installed to ~/.local/bin"
fi

#################
# btop (system monitor)
#################
if ! command -v btop >/dev/null 2>&1; then
    echo "📦 Installing btop..."
    mkdir -p "$HOME/.local/bin"
    BTOP_VERSION=$(curl -s https://api.github.com/repos/aristocratos/btop/releases/latest | grep '"tag_name"' | sed -E 's/.*"v([^"]+)".*/\1/')
    if [ -n "$BTOP_VERSION" ]; then
        BTOP_URL_BASE="https://github.com/aristocratos/btop/releases/download/v${BTOP_VERSION}"
    else
        BTOP_URL_BASE="https://github.com/aristocratos/btop/releases/latest/download"
    fi
    BTOP_ASSET_1="btop-x86_64-linux-musl.tbz"
    BTOP_ASSET_2="btop-x86_64-unknown-linux-musl.tbz"
    if wget -q "${BTOP_URL_BASE}/${BTOP_ASSET_1}" -O /tmp/btop.tbz; then
        :
    elif wget -q "${BTOP_URL_BASE}/${BTOP_ASSET_2}" -O /tmp/btop.tbz; then
        :
    else
        echo "⚠️  Could not download btop from ${BTOP_URL_BASE}"
        echo "   Tried ${BTOP_ASSET_1} and ${BTOP_ASSET_2}"
        rm -f /tmp/btop.tbz
        BTOP_VERSION=""
    fi
    if [ -s /tmp/btop.tbz ]; then
        tar -xjf /tmp/btop.tbz -C /tmp
        mv /tmp/btop/bin/btop "$HOME/.local/bin/"
        chmod +x "$HOME/.local/bin/btop"
        rm -rf /tmp/btop.tbz /tmp/btop
        echo "✅ btop installed to ~/.local/bin"
    fi
fi

#################
# Bitwarden CLI (bw)
#################
if ! command -v bw >/dev/null 2>&1; then
    echo "📦 Installing Bitwarden CLI (bw)..."
    mkdir -p "$HOME/.local/bin"
    BW_VERSION=$(curl -s https://api.github.com/repos/bitwarden/cli/releases/latest | grep '"tag_name"' | sed -E 's/.*"v([^"]+)".*/\1/')
    if [ -n "$BW_VERSION" ]; then
        BW_URL="https://github.com/bitwarden/cli/releases/download/v${BW_VERSION}/bw-linux-${BW_VERSION}.zip"
    else
        BW_URL="https://github.com/bitwarden/cli/releases/latest/download/bw-linux.zip"
    fi
    if ! curl -fsSL "$BW_URL" -o /tmp/bw.zip; then
        echo "⚠️  Could not download Bitwarden CLI from ${BW_URL}"
        rm -f /tmp/bw.zip
    fi
    if [ -s /tmp/bw.zip ]; then
    rm -rf /tmp/bw
    mkdir -p /tmp/bw
    unzip -o /tmp/bw.zip -d /tmp/bw >/dev/null 2>&1
    mv /tmp/bw/bw "$HOME/.local/bin/bw"
    chmod +x "$HOME/.local/bin/bw"
    rm -rf /tmp/bw /tmp/bw.zip
    echo "✅ Bitwarden CLI installed to ~/.local/bin"
    fi
fi

#################
# worktrunk (wt)
#################
if ! command -v wt >/dev/null 2>&1; then
    echo "📦 Installing worktrunk (wt)..."

    # worktrunk is distributed as a Rust crate and requires a modern rustc.
    ensure_modern_rust=false
    if command -v rustc >/dev/null 2>&1; then
        RUSTC_VERSION=$(rustc --version 2>/dev/null | awk '{print $2}')
        if [ -n "$RUSTC_VERSION" ] && [ "$(printf '%s\n' "1.89.0" "$RUSTC_VERSION" | sort -V | head -n1)" = "1.89.0" ]; then
            ensure_modern_rust=true
        fi
    fi

    # If rust toolchain is too old (common on Ubuntu apt), install rustup stable.
    if [ "$ensure_modern_rust" = false ] || ! command -v cargo >/dev/null 2>&1; then
        if ! command -v rustup >/dev/null 2>&1; then
            echo "📦 Installing rustup (required by worktrunk)..."
            curl --proto '=https' --tlsv1.2 -sSf https://sh.rustup.rs | sh -s -- -y --profile minimal --default-toolchain stable
        fi

        if [ -f "$HOME/.cargo/env" ]; then
            # shellcheck source=/dev/null
            . "$HOME/.cargo/env"
        fi
        export PATH="$HOME/.cargo/bin:$PATH"

        if command -v rustup >/dev/null 2>&1; then
            rustup toolchain install stable --profile minimal >/dev/null 2>&1 || true
            rustup default stable >/dev/null 2>&1 || true
        fi
    fi

    if command -v cargo >/dev/null 2>&1; then
        if cargo install worktrunk; then
            echo "✅ worktrunk installed (wt)"
        elif cargo install worktrunk --version 0.1.5; then
            echo "✅ worktrunk 0.1.5 installed as fallback (older rustc compatibility)"
        else
            echo "⚠️  worktrunk installation failed via cargo"
        fi
    else
        echo "⚠️  cargo is unavailable. Skipping worktrunk."
    fi
else
    echo "✅ worktrunk is already installed"
fi

#################
# Oh My Tmux
#################
OHMY_TMUX_BASE="${XDG_CONFIG_HOME:-$HOME/.config}/tmux"
OHMY_TMUX_DIR="$OHMY_TMUX_BASE/oh-my-tmux"
echo "📦 Installing Oh My Tmux (force overwrite)..."
rm -f "$OHMY_TMUX_BASE/tmux.conf" "$OHMY_TMUX_BASE/tmux.conf.local" "$HOME/.tmux.conf"
rm -rf "$OHMY_TMUX_DIR"
mkdir -p "$OHMY_TMUX_BASE"
git clone --single-branch https://github.com/gpakosz/.tmux.git "$OHMY_TMUX_DIR"
ln -sf "$OHMY_TMUX_DIR/.tmux.conf" "$OHMY_TMUX_BASE/tmux.conf"
cp -f "$OHMY_TMUX_DIR/.tmux.conf.local" "$OHMY_TMUX_BASE/tmux.conf.local"
echo "✅ Oh My Tmux installed to $OHMY_TMUX_BASE"

#################
# Create symlinks for renamed commands
#################
echo "🔗 Creating symlinks for commands with different names on Ubuntu..."

mkdir -p "$HOME/.local/bin"

# bat -> batcat
if command -v batcat >/dev/null 2>&1 && ! command -v bat >/dev/null 2>&1; then
    ln -sf "$(which batcat)" "$HOME/.local/bin/bat"
fi

# fd -> fdfind
if command -v fdfind >/dev/null 2>&1 && ! command -v fd >/dev/null 2>&1; then
    ln -sf "$(which fdfind)" "$HOME/.local/bin/fd"
fi

echo "✅ Linux setup complete."

{{ end -}}
