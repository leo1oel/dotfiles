{{ if .nemo_root -}}
# nemo orchestrator location (from the chezmoi `nemo_root` prompt). `nemo` reads NEMO_DIR;
# its projects/ clones live here, so it stays off a small ~.
set -gx NEMO_DIR {{ printf "%s/nemo" .nemo_root | quote }}
{{ end -}}
