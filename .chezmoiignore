# Repo Docs (read from the repo, NOT deployed to $HOME)
README.md
LICENSE
# Anchored with a leading "/" so it only ignores ~/CLAUDE.md (this repo's own
# project-instructions doc) and does NOT touch ~/.claude/CLAUDE.md, which is a
# managed symlink -> ~/AGENTS.md (see dot_claude/symlink_CLAUDE.md.tmpl).
# NOTE: AGENTS.md is intentionally NOT ignored - it deploys to ~/AGENTS.md.
/CLAUDE.md

# Personal secrets
.config/fish/fish_variables

# Lock files
.config/nvim/lazy-lock.json
.config/nvim/lazyvim.json

# Machine-specific files
.gitconfig.local

# OS metadata
.DS_Store

# History files
.history

# DirEnv files
.envrc
.env

# Ignore macOS specific files on non-Macs
{{ if ne .chezmoi.os "darwin" }}
.config/Brewfile
{{ end }}

# Ignore Linux specific files on non-Linux machines
{{ if ne .chezmoi.os "linux" }}
.config/packages.ubuntu
{{ end }}
